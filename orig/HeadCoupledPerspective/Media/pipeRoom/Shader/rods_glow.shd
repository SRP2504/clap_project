diffuseTex = texture( name="texture_DM", uv=uv0 * (15,10))
specularTex = texture( name="texture_SM", uv=uv0 * (15,10))
normal =  (texture( name="texture_NM", uv=uv0 * (15,10)) *2 -1)
specularTmp = diffuseTex * 10
specular = specularTmp * specularTex
ambientTex = texture( name="texture_LM", uv=uv1 * 1 ).r
diffuse = (diffuseTex * diffuseTex) * 20
ambient = diffuse